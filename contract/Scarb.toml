[package]
name = "semaphore_organization_pooling"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = "2.11.4"

[[target.starknet-contract]]
sierra = true
casm = true

[tool.snforge]
exit_first = true
